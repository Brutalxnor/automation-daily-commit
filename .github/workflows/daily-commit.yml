name: Daily contribution update

on:
  workflow_dispatch:
  schedule:
    # Every day at 07:00 Cairo time (~05:00 UTC)
    - cron: "0 5 * * *"

jobs:
  update-readme:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          persist-credentials: true

      - name: Update README timestamp
        run: |
          # Use a more robust date format and ensure the line exists
          TIMESTAMP=$(date -u '+%Y-%m-%d %H:%M:%S')
          LINE="Last update: $TIMESTAMP UTC"

          if [ -f README.md ]; then
            if grep -q '^Last update:' README.md; then
              sed -i "s/^Last update:.*/$LINE/" README.md
            else
              echo -e "\n$LINE" >> README.md
            fi
          else
            echo "# Daily Contributions Automation" > README.md
            echo -e "\n$LINE" >> README.md
          fi

      - name: Commit and push changes
        run: |
          # Use the verified email for contribution attribution
          git config user.name "moshaosama"
          git config user.email "mohamedOSFekry@gmail.com"

          if git diff --quiet; then
            echo "No changes to commit"
            exit 0
          fi

          git add README.md
          git commit -m "chore: automated daily contribution update ($TIMESTAMP)"
          git push

